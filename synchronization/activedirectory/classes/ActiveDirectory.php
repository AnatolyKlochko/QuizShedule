<?php

namespace mod_quizschedule;

defined('MOODLE_INTERNAL') || die();


class ActiveDirectory {
    
    // ( ‘attr’, ‘value’ ) id, fullname, affiliate : array Employee, inside use “new self()” + “const static”
    public static function get_connection() {}
     
}
